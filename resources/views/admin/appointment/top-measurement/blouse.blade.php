<div class="row mb-3">
    <div class="col-md-6 fw-bold">Bust (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_bust ?? '-' }}</span></div>
    <div class="col-md-6 fw-bold">Blouse Length (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_length ?? '-' }}</span></div>
</div>
<div class="row mb-3">
    <div class="col-md-6 fw-bold">Waist (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_waist ?? '-' }}</span></div>
    <div class="col-md-6 fw-bold">Figure (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_figure ?? '-' }}</span></div>
</div>
<div class="row mb-3">
    <div class="col-md-6 fw-bold">Hips (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_hips ?? '-' }}</span></div>
    <div class="col-md-6 fw-bold">Shoulder (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_shoulder ?? '-' }}</span></div>
</div>
<div class="row mb-3">
    <div class="col-md-6 fw-bold">Sleeve Length (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_sleeve ?? '-' }}</span></div>
    <div class="col-md-6 fw-bold">Arm Hole (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_arm_hole ?? '-' }}</span></div>
</div>
<div class="row mb-3">
    <div class="col-md-6 fw-bold">Lower Arm Girth (inches): <span class="fw-light">{{ $appointment->topMeasurement->measurable->blouse_lower_arm_girth ?? '-' }}</span></div>
</div>
